---
import { Icon } from 'astro-icon';
---

<section class="skills-section">
  <div class="skills-container">
    <header class="section-header">
      <h2>Technologies & Expertise</h2>
      <p class="section-description">
        Des outils modernes pour créer des sites web performants, rapides et optimisés pour votre activité.
      </p>
    </header>

    <div class="skills-grid">
      <div class="skill-card">
        <Icon name="vscode-icons:file-type-astro" size="2.5rem" />
        <div class="skill-info">
          <h3>Astro / React</h3>
          <p>Sites vitrines élégants et ultra-rapides, optimisés pour le référencement et l'intégration d'outils de réservation ou de commande en ligne.</p>
        </div>
      </div>

      <div class="skill-card">
        <Icon name="vscode-icons:file-type-angular" size="2.5rem" />
        <div class="skill-info">
          <h3>Angular / NestJS</h3>
          <p>Applications web complètes pour les entreprises : tableaux de bord, outils internes et systèmes de suivi en temps réel.</p>
        </div>
      </div>

		<div class="skill-card">
			<Icon name="mdi:cloud-sync-outline" size="2.5rem" />
			<div class="skill-info">
				<h3>Maintenance / Déploiement</h3>
				<p>Gestion complète après la mise en ligne : sauvegardes, mises à jour et surveillance pour garder votre site toujours opérationnel.</p>
			</div>
		</div>
    </div>
  </div>
</section>

<style>
  .skills-section {
    width: 100%;
    padding: 3rem 1rem;
  }

  .skills-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-header h2 {
    font-size: clamp(2rem, 5vw, 3rem);
    color: var(--gray-0);
    margin-bottom: 1rem;
  }

  .section-description {
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    color: var(--gray-200);
    line-height: 1.7;
    max-width: 700px;
    margin: 0 auto;
  }

  .skills-grid {
    display: grid;
    gap: 2rem;
    grid-template-columns: 1fr;
  }

  .skill-card {
    display: flex;
    align-items: flex-start;
    gap: 1.5rem;
    padding: 2rem;
    background: var(--gradient-subtle);
    border-radius: 1rem;
    border: 1px solid var(--gray-800);
    transition: all 0.3s ease;
  }

  .skill-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
    border-color: var(--accent-regular);
  }

  .skill-card > :global(svg) {
    flex-shrink: 0;
    margin-top: 0.25rem;
  }

  .skill-info {
    flex: 1;
  }

  .skill-info h3 {
    font-size: clamp(1.25rem, 3vw, 1.5rem);
    color: var(--accent-regular);
    margin-bottom: 0.5rem;
    font-weight: 600;
  }

  .skill-info p {
    font-size: clamp(0.95rem, 2vw, 1.1rem);
    color: var(--gray-200);
    line-height: 1.6;
  }

  /* Responsive */
  @media (max-width: 640px) {
    .skills-section {
      padding: 2rem 1rem;
    }

    .section-header {
      margin-bottom: 2rem;
    }

    .skill-card {
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 1.5rem;
    }

    .skills-grid {
      gap: 1.5rem;
    }
  }

  @media (min-width: 768px) {
    .skills-section {
      padding: 4rem 2rem;
    }

    .skills-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .skill-card:last-child {
      grid-column: 1 / -1;
      max-width: 600px;
      margin: 0 auto;
    }
  }

  @media (min-width: 1024px) {
    .skills-section {
      padding: 5rem 3rem;
    }

    .skills-grid {
      grid-template-columns: repeat(3, 1fr);
    }

    .skill-card:last-child {
      grid-column: auto;
      max-width: none;
    }

    .skill-card {
      padding: 2.5rem;
    }
  }

  /* Dark mode adjustments */
  :root.theme-dark .skill-card {
    background: var(--gray-999);
    border-color: var(--gray-800);
  }

  :root.theme-dark .skill-card:hover {
    border-color: var(--accent-dark);
  }

  :root.theme-dark .skill-info h3 {
    color: var(--accent-dark);
  }
</style>