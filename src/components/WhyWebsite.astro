---
import { Icon } from 'astro-icon';
import CallToAction from './CallToAction.astro';
---

<section class="benefits-section">
  <div class="benefits-container">
    <header class="section-header">
      <h2>Pourquoi un site web pour votre restaurant&nbsp;?</h2>
      <p class="section-description">
        Un site web professionnel n'est pas qu'une simple carte en ligne — c'est votre meilleur commercial, 
        disponible 24h/24 pour attirer de nouveaux clients et valoriser votre établissement.
      </p>
    </header>

    <div class="benefits-grid">
      <article class="benefit-card">
        <div class="benefit-icon">
          <Icon name="mdi:map-marker-outline" size="2.5rem" />
        </div>
        <div class="benefit-content">
          <h3>Visibilité locale maximale</h3>
          <p>Apparaissez en tête sur Google et Google Maps. Les clients affamés vous trouvent en un clic, 24h/24.</p>
        </div>
      </article>

      <article class="benefit-card">
        <div class="benefit-icon">
          <Icon name="mdi:food-fork-drink" size="2.5rem" />
        </div>
        <div class="benefit-content">
          <h3>Menu attractif et moderne</h3>
          <p>Présentez vos plats avec de belles photos et un design élégant, lisible sur tous les appareils.</p>
        </div>
      </article>

      <article class="benefit-card">
        <div class="benefit-icon">
          <Icon name="mdi:calendar-check-outline" size="2.5rem" />
        </div>
        <div class="benefit-content">
          <h3>Réservations simplifiées</h3>
          <p>Intégration de système de réservation en ligne ou bouton WhatsApp pour un contact instantané.</p>
        </div>
      </article>

      <article class="benefit-card">
        <div class="benefit-icon">
          <Icon name="mdi:currency-eur" size="2.5rem" />
        </div>
        <div class="benefit-content">
          <h3>Investissement rentable</h3>
          <p>Retour sur investissement rapide avec un tarif transparent, sans abonnement ni frais cachés.</p>
        </div>
      </article>
    </div>

    <div class="cta-wrapper">
      <CallToAction href="mailto:benjamin.melinette@gmail.com">
        Demander un devis gratuit
        <Icon name="ph:paper-plane-tilt" size="1.2em" />
      </CallToAction>
    </div>
  </div>
</section>

<style>
  .benefits-section {
    width: 100%;
    padding: 4rem 1rem;
  }

  .benefits-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3.5rem;
  }

  .section-header h2 {
    font-size: clamp(2rem, 5vw, 3rem);
    color: var(--gray-0);
    margin-bottom: 1rem;
    line-height: 1.2;
  }

  .section-description {
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    color: var(--gray-200);
    line-height: 1.7;
    max-width: 800px;
    margin: 0 auto;
  }

  .benefits-grid {
    display: grid;
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .benefit-card {
    display: flex;
    align-items: flex-start;
    gap: 1.5rem;
    padding: 2rem;
    background: var(--gradient-subtle);
    border-radius: 1rem;
    border: 1px solid var(--gray-800);
    transition: all 0.3s ease;
  }

  .benefit-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
    border-color: var(--accent-regular);
  }

  .benefit-icon {
    flex-shrink: 0;
    color: var(--accent-regular);
    margin-top: 0.25rem;
  }

  .benefit-content {
    flex: 1;
  }

  .benefit-content h3 {
    font-size: clamp(1.25rem, 3vw, 1.5rem);
    color: var(--gray-0);
    margin-bottom: 0.5rem;
    font-weight: 600;
  }

  .benefit-content p {
    font-size: clamp(0.95rem, 2vw, 1.1rem);
    color: var(--gray-200);
    line-height: 1.65;
  }

  .cta-wrapper {
    text-align: center;
    margin-top: 2rem;
  }

  /* Responsive */
  @media (max-width: 640px) {
    .benefits-section {
      padding: 3rem 1rem;
    }

    .section-header {
      margin-bottom: 2.5rem;
    }

    .benefit-card {
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 1.5rem;
    }

    .benefits-grid {
      gap: 1.5rem;
      margin-bottom: 2rem;
    }
  }

  @media (min-width: 768px) {
    .benefits-section {
      padding: 5rem 2rem;
    }

    .benefits-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }
  }

  @media (min-width: 1024px) {
    .benefits-section {
      padding: 6rem 3rem;
    }

    .benefits-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2.5rem;
    }

    .benefit-card {
      padding: 2.5rem;
    }
  }

  /* Dark mode adjustments */
  :root.theme-dark .benefit-card {
    background: var(--gray-999);
    border-color: var(--gray-800);
  }

  :root.theme-dark .benefit-card:hover {
    border-color: var(--accent-dark);
  }

  :root.theme-dark .benefit-icon {
    color: var(--accent-dark);
  }
</style>